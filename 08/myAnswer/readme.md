## Learnings

Two ways to do this:

1. Using `depends_on` attribute as shown [in this docker compose yaml](../../01/myAnswer/docker-compose.yaml)
2. Add sleep before starting service as shown in this [dockerfile](./Dockerfile)
